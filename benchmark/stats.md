# 补充数据采集结果

> 采集时间：2026-05-06 15:40:38
> 采集工具：collect_stats.sh
> 压测参数：wrk -t4 -c100 -d30s

---

## 1. 内存占用

| 框架             | 空载内存 | 满载内存 | 满载 CPU |
| ---------------- | -------- | -------- | -------- |
| Hical (C++)      | 12.94MiB | 13.29MiB | 0.00%    |
| Gin (Go)         | 49.68MiB | 53.39MiB | 0.00%    |
| Actix-web (Rust) | 10.73MiB | 10.73MiB | 0.02%    |

> 空载：服务启动后无请求压力。满载：三个服务同时被 wrk 压测时采样（5s 时刻）。

---

## 2. 二进制 & 镜像大小

| 框架             | 二进制大小 | Docker 镜像大小 |
| ---------------- | ---------- | --------------- |
| Hical (C++)      | 9.2M       | 87.8MB          |
| Gin (Go)         | 7.6M       | 15.3MB          |
| Actix-web (Rust) | 3.7M       | 78.6MB          |

---

## 3. 代码行数（wc -l，含注释和空行）

| 框架             | 文件              | 行数 |
| ---------------- | ----------------- | ---: |
| Hical (C++)      | hical/main.cpp    |   54 |
| Gin (Go)         | gin/main.go       |   52 |
| Actix-web (Rust) | actix/src/main.rs |   64 |

---

> 数据采集于 Docker 容器环境（每容器 4 CPU / 512MB 限制）。
> 内存数据来自 `docker stats --no-stream`，为容器级 RSS。
> 二进制大小为容器内 `/server` 文件，镜像大小来自 `docker images`。
